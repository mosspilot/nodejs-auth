extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
    // TODO: Create a common user model
  if (user)
    if (user.name)
      p You are currently logged in as #{user.name}
    else 
      p You are currently logged in as #{user.username}
    a(href="/logout") Logout